type FieldOptions = {
    variableName: string;
};
/**
 * Returns JS fragment for validating the field to be an array
 */
export declare function validateArrayField({ variableName }: FieldOptions): string;
export {};
